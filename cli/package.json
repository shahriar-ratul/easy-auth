{
  "name": "easy-auth-cli",
  "private": true,
  "version": "0.0.0",
  "description": "npx easy-auth add <combo> — copies registry source into a consumer repo. Not installed as a runtime dependency by anyone.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "cli": "tsx easy-auth.ts"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "tsx": "^4.23.11",
    "typescript": "^7.0.2"
  }
}
